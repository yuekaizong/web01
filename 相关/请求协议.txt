1，今日页面发请求，获取服务器时间和日期
分时和5日
1，请求type=0， 分时为响应lenght=240，每分钟一个价格变化
2，请求type=5， 5日响应为lenght=300，每4分钟一个价格变化
日K月K周K
1，发KLine请求，带requestDate字段，用于判断从获取某个时间点以前的Kline数据，
2，响应length为数据总长度，count为数据当此返回的长度，